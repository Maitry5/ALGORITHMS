def foo():
    print("HELLO FOO")
    

print(foo())

'''Exactly! In Python, every function or method returns a value.

If you explicitly use a return statement with a value, that value is returned.
If you use return without a value, it returns None.
If you don’t specify a return statement at all, the function implicitly returns None.
So yes, if your method doesn’t have a return or just prints something, it still returns None by default.

'''